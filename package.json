{
  "name": "pattern-ledger",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm -r --parallel --stream --filter web --filter server run dev",
    "dev:web": "pnpm --filter web run dev",
    "dev:server": "pnpm --filter server run dev",
    "build": "pnpm -r --filter web --filter server run build",
    "lint": "pnpm -r --filter web --filter server run lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
